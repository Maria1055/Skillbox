# Search Engine — Поисковый движок на Java

Локальный поисковый движок, реализующий многопоточный обход сайтов, индексацию текстового контента с учетом морфологии и ранжирование результатов по релевантности.

## ✨ Особенности проекта
- **Многопоточный обход**: Рекурсивная индексация сайтов через `ForkJoinPool` и `RecursiveAction`.
- **Промышленная база данных**: Полный переход на **PostgreSQL** для высокой стабильности под нагрузкой.
- **Контроль версий БД**: Использование **Liquibase** для автоматического управления схемой данных (миграции).
- **Чистая архитектура**: Разделение ответственности между сервисами (`PageIndexer`, `SiteService`), отсутствие циклических зависимостей.
- **Умная морфология**: Использование библиотек Lucene Morphology для работы с леммами.
- **Стабильность**: Реализовано разделение транзакций (`Propagation.REQUIRES_NEW`) и оптимизация параллелизма для предотвращения Deadlocks.
- **Релевантность**: Ранжирование результатов с расчетом абсолютной и относительной релевантности.

## 🛠 Стек технологий
- **Java 17**
- **Spring Boot 3** (Data JPA, Web)
- **PostgreSQL 14+**
- **Liquibase** (Database Migrations)
- **Jsoup** (Parsing HTML)
- **Lucene Morphology** (Lemmatization)
- **Lombok**
- **Thymeleaf** (Frontend)

## ⚙️ Требования
- **PostgreSQL Server** (убедитесь, что база создана перед запуском).
- **Maven** для сборки проекта.

## 🚀 Локальный запуск

### 1. Подготовка базы данных
Создайте пустую базу данных в PostgreSQL:
```sql
CREATE DATABASE search_engine;
