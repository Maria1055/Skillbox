# Search Engine — Поисковый движок на Java

Локальный поисковый движок, реализующий многопоточный обход сайтов, индексацию текстового контента с учетом морфологии и ранжирование результатов по релевантности.

## ✨ Особенности проекта
- **Многопоточный обход**: Рекурсивная индексация сайтов через `ForkJoinPool` и `RecursiveAction`.
- **Умная морфология**: Использование библиотек Lucene Morphology для работы с леммами (базовыми формами слов).
- **Высокая точность**: Учет границ слов (`regex`), поддержка буквы «ё» и сложных слов с дефисами.
- **Стабильность**: Реализовано разделение транзакций (`Propagation.REQUIRES_NEW`) и микро-паузы для стабильной работы MySQL.
- **Релевантность**: Ранжирование результатов поиска с расчетом абсолютной и относительной релевантности.
- **Сниппеты**: Генерация фрагментов текста с подсветкой поискового запроса и защитой от ложных срабатываний.

## 🛠 Стек технологий
- **Java 17**
- **Spring Boot 3** (Data JPA, Web)
- **MySQL 8.0**
- **Jsoup** (парсинг и скачивание HTML)
- **Lucene Morphology** (лемматизация)
- **Lombok**
- **Thymeleaf** (Frontend)

## ⚙️ Требования
- **MySQL Server 8.0**
- **Maven** для сборки проекта.

## 🚀 Локальный запуск

### 1. Подготовка базы данных
Создайте новую схему в MySQL:
```sql
CREATE DATABASE search_engine;
